# Production docker-compose file (builds static site)
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: geiger-website
    ports:
      - "8080:80"
    restart: unless-stopped

